@model MobileHome.Insure.Web.Models.Admin.ListStateViewModel


<div class="portlet-title"><div class="caption">Add / Edit State</div></div>
<div class="portlet-body">
    <form method="post" id="frmState" action="_partialEditState" class="form-horizontal" role="form">
        <div class="form-body">


            @Html.HiddenFor(x => x.stateObj.Id)


            <div class="form-group">
                @Html.LabelFor(x => x.stateObj.Name, new { @class = "control-label col-md-3" })
                <div class="col-md-9">
                    @Html.TextBoxFor(x => x.stateObj.Name, new { @class = "form-control input-medium" })
                    @Html.ValidationMessageFor(x => x.stateObj.Name, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(x => x.stateObj.Abbr, new { @class = "control-label col-md-3" })
                <div class="col-md-9">
                    @Html.TextBoxFor(x => x.stateObj.Abbr, new { @class = "form-control input-medium" })
                    @Html.ValidationMessageFor(x => x.stateObj.Abbr, "", new { @class = "text-danger" })

                </div>

            </div>

        </div>

        <div class="row">
            &nbsp;
        </div>
        <div class="row">
            <div class="col-md-4 col-md-offset-4">
                <button type="button" class="btn btn-default"
                        data-dismiss="modal">
                    Cancel
                </button>
                <button type="submit" id="approve-btn"
                        class="btn btn-danger">
                    Save
                </button>
            </div>
        </div>


    </form>
</div>
<script type="text/javascript">
    $(function () {
        $('#frmState').submit(function () {
            if ($(this).valid())
                $('#modal-container').modal('hide');
        });
        $(".select2me").select2();
        $.validator.unobtrusive.parse("#frmState");
    });

</script>