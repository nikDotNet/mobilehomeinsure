@model MobileHome.Insure.Web.Models.Admin.ListNotifyViewModel


<div class="portlet-title"><div class="caption">Add / Edit Notify</div></div>
<div class="portlet-body">
    <form method="post" id="frmNotify" action="_partialEditNotify" class="form-horizontal" role="form">
        <div class="form-body">


            @Html.HiddenFor(x => x.notifyObj.Id)


            <div class="form-group">
                @Html.LabelFor(x => x.notifyObj.Name, new { @class = "control-label col-md-3" })
                <div class="col-md-9">
                    @Html.TextBoxFor(x => x.notifyObj.Name, new { @class = "form-control input-medium" })
                    @Html.ValidationMessageFor(x => x.notifyObj.Name, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(x => x.notifyObj.Email, new { @class = "control-label col-md-3" })
                <div class="col-md-9">
                    @Html.TextBoxFor(x => x.notifyObj.Email, new { @class = "form-control input-medium" })
                    @Html.ValidationMessageFor(x => x.notifyObj.Email, "", new { @class = "text-danger" })

                </div>

            </div>

            <div class="form-group">
                @Html.LabelFor(x => x.notifyObj.Zip, new { @class = "control-label col-md-3" })
                <div class="col-md-9">
                    @Html.TextBoxFor(x => x.notifyObj.Zip, new { @class = "form-control input-medium" })
                    @Html.ValidationMessageFor(x => x.notifyObj.Zip, "", new { @class = "text-danger" })

                </div>

            </div>

        </div>

        <div class="row">
            &nbsp;
        </div>
        <div class="row">
            <div class="col-md-4 col-md-offset-4">
                <button type="button" class="btn btn-default"
                        data-dismiss="modal">
                    Cancel
                </button>
                <button type="submit" id="approve-btn"
                        class="btn btn-danger">
                    Save
                </button>
            </div>
        </div>


    </form>
</div>
<script type="text/javascript">
    $(function () {
        $('#frmNotify').submit(function () {
            if ($(this).valid())
                $('#modal-container').modal('hide');
        });
        $(".select2me").select2();
        $.validator.unobtrusive.parse("#frmNotify");
    });

</script>